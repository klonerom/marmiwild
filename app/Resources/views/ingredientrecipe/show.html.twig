{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ingredientrecipe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredientRecipe.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ ingredientRecipe.quantity }}</td>
            </tr>
            <tr>
                <th>Serving</th>
                <td>{{ ingredientRecipe.serving }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ingredients-recipe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ingredients-recipe_edit', { 'id': ingredientRecipe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
