{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorierecipe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieRecipe.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorieRecipe.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categories-recipe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categories-recipe_edit', { 'id': categorieRecipe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
